diff --git a/apps/cli/src/index.ts b/apps/cli/src/index.ts
index 492f143..0000000 100644
--- a/apps/cli/src/index.ts
+++ b/apps/cli/src/index.ts
@@ -40,7 +40,10 @@ import {
   warning,
 } from './terminal';
 
-const CLI_VERSION = '0.1.0';
+declare const __AIRRUN_VERSION__:
+  string;
+
+const CLI_VERSION = __AIRRUN_VERSION__;
 
 async function main(): Promise<void> {
   const args =
diff --git a/apps/cli/esbuild.mjs b/apps/cli/esbuild.mjs
index a5c4924..0000000 100644
--- a/apps/cli/esbuild.mjs
+++ b/apps/cli/esbuild.mjs
@@ -2,6 +2,7 @@ import {
   chmod,
   mkdir,
+  readFile,
 } from 'node:fs/promises';
 
 import {
@@ -18,6 +19,29 @@ const outputFile =
     import.meta.url,
   );
 
+const packageJson =
+  JSON.parse(
+    await readFile(
+      new URL(
+        './package.json',
+        import.meta.url,
+      ),
+      'utf8',
+    ),
+  );
+
+const cliVersion =
+  packageJson.version;
+
+if (
+  typeof cliVersion !== 'string' ||
+  !cliVersion.trim()
+) {
+  throw new Error(
+    'La version de la CLI est invalide.',
+  );
+}
+
 await mkdir(
   outputDirectory,
   {
@@ -38,6 +62,10 @@ await build({
   format: 'cjs',
   target: 'node20',
   sourcemap: true,
+  define: {
+    __AIRRUN_VERSION__:
+      JSON.stringify(cliVersion),
+  },
   banner: {
     js: '#!/usr/bin/env node',
   },
diff --git a/packages/core/src/devices/flutterDeviceService.ts b/packages/core/src/devices/flutterDeviceService.ts
index 9b9a916..0000000 100644
--- a/packages/core/src/devices/flutterDeviceService.ts
+++ b/packages/core/src/devices/flutterDeviceService.ts
@@ -45,7 +45,7 @@ export class FlutterDeviceService {
       flutterPath,
       ['devices', '--machine'],
       {
-        timeoutMs: 30_000,
+        timeoutMs: 90_000,
       },
     );
 
diff --git a/packages/core/src/wireless/pairingService.ts b/packages/core/src/wireless/pairingService.ts
index ad2e44d..0000000 100644
--- a/packages/core/src/wireless/pairingService.ts
+++ b/packages/core/src/wireless/pairingService.ts
@@ -121,17 +121,25 @@ export class PairingService {
     }
 
     /*
-     * Le secret n’apparaît pas dans les arguments
-     * du processus. Il passe uniquement par stdin.
+     * ADB accepte officiellement le secret comme
+     * troisième argument de « adb pair ».
+     *
+     * Cette forme évite les erreurs de protocole
+     * observées lorsque stdin n’est pas attaché à
+     * un terminal interactif.
      */
     const result = await this.processRunner.run(
       adbPath,
-      ['pair', service.endpoint],
+      [
+        'pair',
+        service.endpoint,
+        normalizedSecret,
+      ],
       {
         timeoutMs:
           options.timeoutMs ?? 30_000,
-        stdin: `${normalizedSecret}\n`,
         ...(options.signal
           ? {
               signal: options.signal,
diff --git a/packages/core/src/wireless/qrPairingService.ts b/packages/core/src/wireless/qrPairingService.ts
index a141704..0000000 100644
--- a/packages/core/src/wireless/qrPairingService.ts
+++ b/packages/core/src/wireless/qrPairingService.ts
@@ -13,14 +13,10 @@ import {
 } from './pairingService';
 
 import {
+  MdnsServiceParser,
   type AdbMdnsService,
 } from './mdnsService';
 
-import {
-  MdnsTrackServiceParser,
-  type TrackedAdbMdnsService,
-} from './mdnsTrackService';
-
 export type QrPairingStage =
   | 'waiting-for-scan'
   | 'service-found'
@@ -47,8 +43,8 @@ export class QrPairingService {
       new ExecutableLocator(),
     private readonly processRunner =
       new ProcessRunner(),
-    private readonly trackParser =
-      new MdnsTrackServiceParser(),
+    private readonly mdnsParser =
+      new MdnsServiceParser(),
     private readonly pairingService =
       new PairingService(),
   ) {}
@@ -124,146 +120,83 @@ export class QrPairingService {
     expectedServiceName: string,
     timeoutMs: number,
     signal?: AbortSignal,
-  ): Promise<TrackedAdbMdnsService> {
-    const controller =
-      new AbortController();
-
-    const forwardCancellation =
-      (): void => {
-        controller.abort();
-      };
-
-    if (signal) {
-      if (signal.aborted) {
-        throw new Error(
-          'Association QR annulée.',
-        );
-      }
-
-      signal.addEventListener(
-        'abort',
-        forwardCancellation,
-        {
-          once: true,
-        },
-      );
-    }
-
-    let outputBuffer = '';
-
-    let foundService:
-      TrackedAdbMdnsService | null =
-        null;
-
-    const inspectChunk = (
-      chunk: string,
-    ): void => {
-      outputBuffer += chunk;
-
-      /*
-       * Le flux peut rester ouvert longtemps.
-       * On conserve une fenêtre suffisamment
-       * grande pour contenir plusieurs snapshots.
-       */
-      if (
-        outputBuffer.length >
-        512_000
-      ) {
-        outputBuffer =
-          outputBuffer.slice(
-            -256_000,
-          );
-      }
-
-      const services =
-        this.trackParser.parse(
-          outputBuffer,
-        );
-
-      const match =
-        services.find(service =>
-          this.matchesService(
-            service,
-            expectedServiceName,
-          ),
-        );
-
-      if (!match) {
-        return;
-      }
-
-      foundService = match;
-
-      /*
-       * La découverte est terminée :
-       * on arrête uniquement le processus
-       * track-services.
-       */
-      controller.abort();
-    };
-
-    try {
+  ): Promise<AdbMdnsService> {
+    const deadline =
+      Date.now() + timeoutMs;
+
+    let lastDiagnostic = '';
+
+    while (Date.now() < deadline) {
+      this.throwIfAborted(signal);
+
+      const remainingMs =
+        Math.max(
+          1,
+          deadline - Date.now(),
+        );
+
       const result =
         await this.processRunner.run(
           adbPath,
-          [
-            'mdns',
-            'track-services',
-            '--proto-text',
-          ],
+          ['mdns', 'services'],
           {
-            timeoutMs,
-            signal:
-              controller.signal,
-            onStdout: inspectChunk,
-            onStderr: inspectChunk,
+            timeoutMs:
+              Math.min(
+                10_000,
+                remainingMs,
+              ),
+            ...(signal
+              ? {
+                  signal,
+                }
+              : {}),
           },
         );
 
       this.throwIfAborted(signal);
 
-      if (foundService) {
-        return foundService;
-      }
-
       const combinedOutput =
         [
           result.stdout,
@@ -275,35 +208,43 @@ export class QrPairingService {
           .filter(Boolean)
           .join('\n');
 
-      if (result.timedOut) {
-        throw new Error(
-          'Aucun téléphone n’a scanné le QR dans le délai de deux minutes.',
-        );
-      }
-
       if (
-        result.exitCode !== 0 &&
-        !result.aborted
+        !result.timedOut &&
+        result.exitCode === 0
       ) {
-        throw new Error(
-          `La surveillance mDNS a échoué : ${
-            combinedOutput ||
-            'erreur inconnue'
-          }`,
+        const match =
+          this.mdnsParser
+            .parseServices(
+              result.stdout,
+            )
+            .find(service =>
+              this.matchesService(
+                service,
+                expectedServiceName,
+              ),
+            );
+
+        if (match) {
+          return match;
+        }
+      } else if (combinedOutput) {
+        lastDiagnostic =
+          combinedOutput;
+      }
+
+      const delayMs =
+        Math.min(
+          1_000,
+          Math.max(
+            0,
+            deadline - Date.now(),
+          ),
         );
-      }
 
-      throw new Error(
-        'Le service QR attendu n’a pas été détecté sur le réseau.',
+      if (delayMs > 0) {
+        await this.delay(delayMs);
+      }
+    }
+
+    throw new Error(
+      lastDiagnostic
+        ? [
+            'Aucun téléphone n’a scanné le QR dans le délai de deux minutes.',
+            `Dernier diagnostic ADB : ${lastDiagnostic}`,
+          ].join(' ')
+        : 'Aucun téléphone n’a scanné le QR dans le délai de deux minutes.',
+    );
+  }
+
+  private async delay(
+    durationMs: number,
+  ): Promise<void> {
+    await new Promise<void>(resolve => {
+      setTimeout(
+        resolve,
+        durationMs,
       );
-    } finally {
-      signal?.removeEventListener(
-        'abort',
-        forwardCancellation,
-      );
-    }
+    });
   }
 
   private matchesService(
-    service: TrackedAdbMdnsService,
+    service: AdbMdnsService,
     expectedServiceName: string,
   ): boolean {
     if (
