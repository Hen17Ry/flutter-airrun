{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:flutter-airrun",
      "type": "shell",
      "command": "pnpm --filter flutter-airrun build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
